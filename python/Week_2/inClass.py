# Comments
    # single line comment 
    # Python has no multi-line comments

# Naming Styles

PascalCase # Class - use nouns
    class Customer
    class BankAccount
    def average
    def swap

snake_case # Variables - represent what value is being stored
first_name = "John"
order_name = 1

UPPER_CASE # Constant variables - inital value should not change
DEFAULT_WIDTH = 100
MAX_HEIGHT = 200

# Comparison Operator

x = 5
w = 7
result = x > w # result would be False
result = (x + 2 == w) # result would be True

# Logical operator

if x > 10: 
    pass
else:
    y = y + 1

if not x > 10:
    y = y + 1

# pow function

x = pow(2, 5)
x = 2 ** 5

area = pi * radius ** 2
volume = 4 / 3 * pi * pow(radius, 3)

# Generating random numbers

import random
y = random.randint(0, 10)

# sqrt function

import math
x = math.sqrt(3)


